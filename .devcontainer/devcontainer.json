{
  "image": "ghcr.io/jaimemachadoneto/nix-devcontainer-vscode-node-slim:sha-e5f1f0c",
  "mounts": [
    "source=jaimemachadoneto-dev,target=/nix,type=volume",
		"source=${localEnv:USERPROFILE}/.pi,target=/home/vscode-node-slim/.pi,type=bind,consistency=cached"
  ],
	"postCreateCommand": "nix profile remove home-manager-path && nix profile install nixpkgs#direnv && echo 'eval \"$(direnv hook bash)\"' >> ~/.bashrc && sudo apt update && sudo apt install -y npm && sudo npm install -g @mariozechner/pi-coding-agent",
  "customizations": {}
}
